<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jain.pranjal.studentregestration">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/student_registration_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/student_registration_app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrontPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/myStyle"></activity>
        <activity android:name=".Add" android:theme="@style/myStyle"></activity>
        <activity android:name=".Edit" android:theme="@style/myStyle"></activity>
        <activity android:name=".StudentDetails" android:theme="@style/myStyle"></activity>
        <activity android:name=".CalanderActivity" android:theme="@style/myStyle"></activity>
    </application>

</manifest>